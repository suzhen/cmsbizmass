%ul.employing_unit_list
  - @employing_units.each do |unit|
    %li
      = link_to image_tag(unit.logo.url(:normal),:border => 0, :title=>unit.name),"/zhaopin/#{unit.id.to_s}"
      %span
        = link_to unit.name,"/zhaopin/#{unit.id.to_s}"
  

