%ul.gallery_list
  - @pictures.each do |pic|
    %li
      %div.galleryimgtxt
        %a{href: pic.url} 
          %span.img
            = image_tag pic.picture.url(:size2),:border => 0, :size => "144x122"
          %span.txt
            = pic.title
